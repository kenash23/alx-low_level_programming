#include <stdio.h>
/**
 * main - Entry Point
 *
 * Description: Print lowercase alphabet except q and e
 * Return: 0
 */
int main(void)
{
	int c;

	for (c = 1; c <= 26; ++c)
	{
		if (c = 5 || c = 17)
			break;
		putchar('a' + c);
		putchar('\n');
	}
	Return (0);
}
